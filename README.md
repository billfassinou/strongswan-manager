# strongswan-manager
StrongSwan Manager replaces hand-editing swanctl.conf and the command line with a clear interface: guided IPsec tunnel creation, built-in PKI, real-time monitoring, an automatic security score and a tamper-proof audit log. Everything goes through the official VICI API — not a single configuration file is written by hand.
